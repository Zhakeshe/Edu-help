# 🎓 EduHelp - Білім беру платформасы

Мұғалімдерге арналған материалдар мен AI құралдарды біріктіретін заманауи платформа.

## 🚀 Мүмкіндіктер

### ✅ Негізгі функциялар
- 📚 2-11 сыныптарға арналған материалдар
- 📝 ҚМЖ, презентациялар, жұмыс парақтары
- 🤖 AI құралдар (мәтін және сурет генерациялау)
- 💬 Кері байланыс жүйесі
- 👨‍💼 Админ панель
- 📤 Файл жүктеу жүйесі

### 🎨 Технологиялар

#### Frontend
- React 18
- React Router v6
- TailwindCSS
- Axios
- Lucide Icons

#### Backend
- Node.js
- Express.js
- MongoDB (Mongoose)
- JWT авторизация
- Multer (файл жүктеу)

## 📦 Орнату

### 1. Репозиторийді клондау
```bash
git clone <repository-url>
cd Edu-help
```

### 2. Backend орнату

```bash
cd backend
npm install
```

`.env` файлын жасаңыз:
```env
MONGODB_URI=mongodb://localhost:27017/eduhelp
JWT_SECRET=your_secret_key_here
PORT=5000
FRONTEND_URL=http://localhost:5173

# AI API кілттері (опциялық)
OPENAI_API_KEY=your_key
ANTHROPIC_API_KEY=your_key
GOOGLE_AI_API_KEY=your_key
```

### 3. Frontend орнату

```bash
cd ../frontend
npm install
```

### 4. MongoDB іске қосу

MongoDB қосылуын тексеріңіз:
```bash
mongod
```

### 5. Серверлерді іске қосу

Backend (терминал 1):
```bash
cd backend
npm run dev
```

Frontend (терминал 2):
```bash
cd frontend
npm run dev
```

## 🔐 Алғашқы админ тіркеу

1. Backend іске қосылғаннан кейін POST сұрау жіберіңіз:

```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "email": "admin@eduhelp.kz",
    "password": "admin123"
  }'
```

Немесе Postman/Thunder Client арқылы:
- URL: `http://localhost:5000/api/auth/register`
- Method: POST
- Body (JSON):
```json
{
  "username": "admin",
  "email": "admin@eduhelp.kz",
  "password": "admin123"
}
```

2. Браузерде `/admin/login` бетіне өтіп, кіріңіз.

## 📁 Жоба құрылымы

```
Edu-help/
├── backend/
│   ├── config/
│   │   └── db.js                 # MongoDB қосылуы
│   ├── middleware/
│   │   └── auth.js               # JWT middleware
│   ├── models/
│   │   ├── Admin.js              # Админ моделі
│   │   ├── Material.js           # Материал моделі
│   │   ├── Feedback.js           # Кері байланыс моделі
│   │   └── AITool.js             # AI құрал моделі
│   ├── routes/
│   │   ├── auth.js               # Авторизация роуттары
│   │   ├── materials.js          # Материалдар роуттары
│   │   ├── classes.js            # Сыныптар роуттары
│   │   ├── aitools.js            # AI құралдар роуттары
│   │   └── feedback.js           # Кері байланыс роуттары
│   ├── uploads/                  # Жүктелген файлдар
│   ├── server.js                 # Негізгі сервер
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Navbar.jsx        # Навигация
│   │   │   ├── Hero.jsx          # Басты бет hero
│   │   │   ├── ClassesList.jsx   # Сыныптар тізімі
│   │   │   ├── MaterialsView.jsx # Материалдарды көрсету
│   │   │   ├── AITools.jsx       # AI құралдар
│   │   │   ├── ChatButton.jsx    # Кері байланыс кнопкасы
│   │   │   └── Admin/
│   │   │       ├── Login.jsx           # Админ кіру
│   │   │       ├── Dashboard.jsx       # Админ панель
│   │   │       ├── MaterialUpload.jsx  # Материал жүктеу
│   │   │       └── FeedbackManagement.jsx # Кері байланыс басқару
│   │   ├── pages/
│   │   │   ├── Home.jsx          # Басты бет
│   │   │   ├── ClassPage.jsx     # Сынып беті
│   │   │   ├── AIToolsPage.jsx   # AI құралдар беті
│   │   │   └── AdminPanel.jsx    # Админ панель беті
│   │   ├── context/
│   │   │   └── AuthContext.jsx   # Авторизация контексті
│   │   ├── App.jsx               # Негізгі App
│   │   ├── main.jsx              # Entry point
│   │   └── index.css             # Стильдер
│   ├── package.json
│   ├── tailwind.config.js
│   ├── vite.config.js
│   └── index.html
│
└── README.md
```

## 🔌 API Endpoints

### Авторизация
- `POST /api/auth/register` - Админ тіркеу
- `POST /api/auth/login` - Админ кіру
- `GET /api/auth/me` - Қазіргі админді алу

### Материалдар
- `GET /api/materials` - Барлық материалдарды алу
- `GET /api/materials/:id` - Бір материалды алу
- `POST /api/materials/upload` - Материал жүктеу (Admin)
- `PUT /api/materials/:id` - Материалды жаңарту (Admin)
- `DELETE /api/materials/:id` - Материалды өшіру (Admin)
- `GET /api/materials/download/:id` - Материалды жүктеп алу

### Сыныптар
- `GET /api/classes` - Барлық сыныптарды алу
- `GET /api/classes/:classNumber` - Бір сыныпты алу
- `GET /api/classes/:classNumber/stats` - Сынып статистикасы

### AI құралдар
- `GET /api/ai-tools` - Барлық AI құралдарды алу
- `GET /api/ai-tools/:id` - Бір AI құралды алу
- `POST /api/ai-tools` - AI құрал қосу (Admin)
- `PUT /api/ai-tools/:id` - AI құралды жаңарту (Admin)
- `DELETE /api/ai-tools/:id` - AI құралды өшіру (Admin)
- `POST /api/ai-tools/generate/text` - Мәтін генерациялау

### Кері байланыс
- `POST /api/feedback` - Хабарлама жіберу
- `GET /api/feedback` - Барлық хабарламаларды алу (Admin)
- `GET /api/feedback/:id` - Бір хабарламаны алу (Admin)
- `PUT /api/feedback/:id/respond` - Жауап беру (Admin)
- `DELETE /api/feedback/:id` - Хабарламаны өшіру (Admin)

## 🎯 Пайдалану нұсқаулығы

### Админ үшін

1. **Кіру**: `/admin/login` бетіне өтіп, логин/құпия сөзді енгізіңіз
2. **Материал жүктеу**:
   - "Материалдар жүктеу" бөліміне өтіңіз
   - Тақырып, сынып, тоқсан, категорияны таңдаңыз
   - Файлды таңдап, жүктеңіз
3. **Кері байланысқа жауап беру**:
   - "Кері байланыс" бөліміне өтіңіз
   - Хабарламаны оқып, жауап жазыңыз

### Пайдаланушы үшін

1. **Материал іздеу**:
   - Басты бетте сыныпты таңдаңыз
   - Тоқсанды таңдаңыз
   - Категория бойынша материалдарды көріңіз
2. **AI құралдарды пайдалану**:
   - "AI Құралдар" бетіне өтіңіз
   - Қажетті құралды таңдап, пайдаланыңыз
3. **Кері байланыс**:
   - Оң төменгі бұрыштағы чат кнопкасын басыңыз
   - Хабарламаңызды жіберіңіз

## 🔧 Конфигурация

### MongoDB деректер қорын инициализациялау

AI құралдарды қолмен қосу үшін:

```javascript
// MongoDB shell немесе MongoDB Compass арқылы
db.aitools.insertMany([
  {
    name: "ChatGPT",
    description: "OpenAI-дан мәтін генерациялау құралы",
    category: "Мәтін генерациялау",
    url: "https://chat.openai.com",
    features: ["Мәтін жазу", "Тапсырма жасау", "Түсіндірулер"],
    rating: 5,
    isActive: true
  },
  {
    name: "Midjourney",
    description: "Сапалы суреттер генерациялау",
    category: "Сурет генерациялау",
    url: "https://www.midjourney.com",
    features: ["Сурет жасау", "Иллюстрациялар", "Постер дизайны"],
    rating: 5,
    isPremium: true,
    isActive: true
  }
]);
```

## 🐛 Қателерді жою

### MongoDB қосыла алмаса:
```bash
# MongoDB қызметін іске қосыңыз
sudo systemctl start mongod

# Немесе MacOS үшін
brew services start mongodb-community
```

### Port 5000 бос емес:
`.env` файлында портты өзгертіңіз:
```env
PORT=5001
```

### Frontend backend-қа қосыла алмаса:
`vite.config.js` файлындағы proxy параметрін тексеріңіз.

## 📝 Лицензия

MIT License

## 👥 Авторлар

EduHelp Team

## 📧 Байланыс

Сұрақтар болса: support@eduhelp.kz
